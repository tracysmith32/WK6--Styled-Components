import { Button, Wrapper } from '../styles/Button.styles';

const Buttons = () => {
	return (
		<Wrapper>
			<Button color="white" background="red">
				Add
			</Button>
			<Button color="white" background="green">
				Add
			</Button>
			<Button color="white" background="blue">
				Add
			</Button>
		</Wrapper>
	);
};

export default Buttons;
